<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login - Aura</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>
<body class="bg-pink-50 text-gray-800 font-sans min-h-screen flex flex-col">

    <!-- Header -->
    <header class="bg-white shadow-md py-6 px-8 flex justify-between items-center">
        <h1 class="text-3xl font-bold text-pink-600 px-3 py-1 bg-white rounded">Aura</h1>
        <nav class="space-x-4">
            <a href="/" class="text-gray-700 hover:text-pink-500 font-medium">Home</a>
            <a href="/profile" class="text-gray-700 hover:text-pink-500 font-medium">Profile</a>
            <a href="/crystals" class="text-gray-700 hover:text-pink-500 font-medium">Crystals</a>
            <a href="/logout" class="bg-pink-500 text-white px-4 py-2 rounded hover:bg-pink-600 transition">Logout</a>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="flex-grow flex items-center justify-center">
        <div class="bg-white shadow-md rounded-lg p-8 max-w-md w-full">
            <% if (formType === 'signup') { %>
                <h2 class="text-2xl font-semibold text-center mb-6 text-pink-600">Sign Up</h2>
                <form action="/signup" method="POST" class="space-y-4">
                    <input type="text" name="username" placeholder="Username" required
                        class="w-full px-4 py-2 border rounded-md text-gray-800 focus:outline-pink-500" />
                    <input type="date" name="dob" required
                        class="w-full px-4 py-2 border rounded-md text-gray-800 focus:outline-pink-500" />
                    <input type="password" name="password" placeholder="Password" required
                        class="w-full px-4 py-2 border rounded-md text-gray-800 focus:outline-pink-500" />
                    <input type="password" name="confirmPassword" placeholder="Confirm Password" required
                        class="w-full px-4 py-2 border rounded-md text-gray-800 focus:outline-pink-500" />
                    <button type="submit"
                        class="w-full bg-pink-500 text-white px-4 py-2 rounded hover:bg-pink-600 transition">Sign Up</button>
                </form>
                <p class="text-center text-sm mt-4">Already have an account? 
                    <a href="/login?form=login" class="text-pink-500 hover:text-pink-600">Login here</a>.
                </p>
            <% } else { %>
                <h2 class="text-2xl font-semibold text-center mb-6 text-pink-600">Login</h2>
                <form action="/login" method="POST" class="space-y-4">
                    <input type="text" name="username" placeholder="Username" required
                        class="w-full px-4 py-2 border rounded-md text-gray-800 focus:outline-pink-500" />
                    <input type="password" name="password" placeholder="Password" required
                        class="w-full px-4 py-2 border rounded-md text-gray-800 focus:outline-pink-500" />
                    <button type="submit"
                        class="w-full bg-pink-500 text-white px-4 py-2 rounded hover:bg-pink-600 transition">Login</button>
                </form>
                <p class="text-center text-sm mt-4">Don't have an account? 
                    <a href="/login?form=signup" class="text-pink-500 hover:text-pink-600">Sign up here</a>.
                </p>
            <% } %>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white text-center py-6 text-gray-500 text-sm">
        &copy; <%= new Date().getFullYear() %> Aura — Personalized Jewelry, Aligned with Your Energy
    </footer>

</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= name %>'s Profile | Aura</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>
<body class="bg-pink-50 text-gray-800 font-sans">
    <!-- Header -->
    <header class="bg-white shadow-md py-6 px-8 flex justify-between items-center">
        <h1 class="text-3xl font-bold text-pink-600">Aura</h1>
        <nav class="space-x-4">
            <a href="/" class="text-gray-700 hover:text-pink-500 font-medium">Home</a>
            <a href="/profile" class="text-gray-700 hover:text-pink-500 font-medium">Profile</a>
            <a href="/crystals" class="text-gray-700 hover:text-pink-500 font-medium">Crystals</a>
            <a href="/logout" class="bg-pink-500 text-white px-4 py-2 rounded hover:bg-pink-600 transition">Logout</a>
        </nav>
    </header>

    <!-- Profile Content -->
    <main class="max-w-4xl mx-auto py-10 px-6 bg-white shadow-md rounded-lg mt-10">
        <h2 class="text-2xl font-semibold text-pink-600 mb-6"><%= name %>'s Profile</h2>

<!-- Profile Photo Upload Section START -->
<section class="mb-10 flex items-center space-x-8">
    <!-- Display profile photo or placeholder -->
    <div class="w-32 h-32 rounded-full overflow-hidden border-4 border-pink-300 shadow-md">
        <% if (profileImage) { %>
            <img src="<%= profileImage %>" alt="Profile Photo" class="w-full h-full object-cover object-center">
        <% } else { %>
            <img src="/images/default-avatar.png" alt="Default Profile Photo" class="w-full h-full object-cover object-center">
        <% } %>
    </div>


    <!-- Upload form -->
    <form action="/profile/upload" method="POST" enctype="multipart/form-data" class="space-y-2">
      <label for="profilePhoto" class="block text-sm font-medium text-gray-700">Upload New Profile Photo</label>
      <input id="profilePhoto" type="file" name="profilePhoto" accept="image/*" required
             class="block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded file:border-0 file:text-sm file:font-semibold file:bg-pink-50 file:text-pink-700 hover:file:bg-pink-100">      
        <button type="submit" class="mt-2 bg-pink-500 text-white px-4 py-2 rounded hover:bg-pink-600 transition">
            Upload
        </button>
    </form>
</section>
<!-- Profile Photo Upload Section END -->

        <!-- User Info Section -->
        <section class="mb-8">
            <h3 class="text-lg font-medium text-gray-700 mb-2">Birthday</h3>
            <p class="text-gray-800 bg-pink-100 px-4 py-2 rounded w-fit"><%= birthday %></p>
        </section>
      

    </main>
    <!-- Footer -->
    <footer class="bg-white text-center py-6 mt-10 text-gray-500 text-sm">
        &copy; <%= new Date().getFullYear() %> Aura — Personalized Jewelry, Aligned with Your Energy
    </footer>

</body>
</html>
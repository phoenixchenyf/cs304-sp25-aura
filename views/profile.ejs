<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/3.0.3/normalize.min.css">
    <link rel="stylesheet" href="/profile.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <meta name=author content="Joelle Liu">
    <title>User Profile</title>
</head>

<body>
  <header>
      <h1>Your Aura Profile</h1>
      <nav>
          <a href="/">Home</a>
          <a href="/profile">Profile</a>
          <a href="/products">Jewelry Collection</a>
          <a href="/logout">Logout</a>
      </nav>
  </header>

  <main>
      <h2>Welcome, <%= user.user_name %>!</h2>

      <form action="/profile/update" method="POST">
          <label for="birthday">Birthday:</label>
          <input type="date" id="birthday" name="birthday" value="<%= user.birthday %>" required>
          
          <label for="preferences">Preferences:</label>
          <textarea id="preferences" name="preferences" rows="4" cols="50"><%= user.preferences %></textarea>

          <button type="submit">Update Profile</button>
      </form>
  </main>
</body>

</html>